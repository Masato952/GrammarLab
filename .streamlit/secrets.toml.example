# 复制这个文件为 .streamlit/secrets.toml（本地开发用，已加入 .gitignore，不会被提交）。
# 部署到 Streamlit Cloud 时，把同样的三行填到 App 设置里的 "Secrets" 面板，不要提交到仓库里。

GITHUB_TOKEN = "github_pat_xxx"   # 只需要这个仓库的 Contents 读写权限的 fine-grained token
GITHUB_REPO = "your-username/GrammarLab"
GITHUB_BRANCH = "main"
